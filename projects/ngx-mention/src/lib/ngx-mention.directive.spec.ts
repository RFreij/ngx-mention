import { NgxMentionDirective } from './ngx-mention.directive';

describe('NgxMentionDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxMentionDirective();
        expect(directive).toBeTruthy();
    });
});
